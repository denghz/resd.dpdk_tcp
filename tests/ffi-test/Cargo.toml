[package]
name = "ffi-test"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
resd-net = { path = "../../crates/resd-net" }
libc.workspace = true

[build-dependencies]
pkg-config = "0.3"

# T29 bench-pair report — 2026-05-07

**Run:** `2026-05-07T02-28-08Z`
**Branch:** `a10-perf-23.11`
**Commits:** `a1ab7f3` (build.rs DPDK double-link fix), `0dfef53` (T28 report)
**Host:** ec2-13-233-8-113.ap-south-1, c6a.2xlarge, DPDK 23.11.0

## Summary

**DPDK double-link fix confirmed.** bench-vs-mtcp burst and maxtp stderrs show
only a precondition failure — no `RTE_MBUF_DYNFIELD tailq` panic. Fix `a1ab7f3`
is correct: removing the redundant DPDK pkg-config block from build.rs's fstack
path resolves the EAL tailq double-registration.

**All benches failed due to precondition failure.** The EC2 DUT booted with empty
`isolcpus`/`nohz_full`/`rcu_nocbs` kernel parameters despite using the same AMI
(`ami-0e483926d07d19647`) and instance type (`c6a.2xlarge`) as T27 and T28, which
both passed. This is a transient infrastructure fluke, not a code regression.

## Results

**bench-e2e:** exited rc=1 (precondition strict-mode fail) — no data  
**bench-vs-linux:** exited rc=1 (precondition strict-mode fail) — no data  
**bench-vs-mtcp-burst:** exited rc=1 (precondition strict-mode fail) — no data  
**bench-vs-mtcp-maxtp:** exited rc=1 (precondition strict-mode fail) — no data  
**bench-report:** 0 rows passed StrictOnly filter

## DPDK Fix Confirmation

T28 burst/maxtp stderrs (before fix): DPDK panic at startup  
T29 burst/maxtp stderrs (after fix): clean precondition message, no panic

```
# T28 (before a1ab7f3):
EAL: RTE_MBUF_DYNFIELD tailq is already registered
PANIC in tailqinitfn_mbuf_dynfield_tailq()

# T29 (after a1ab7f3):
bench-vs-mtcp: precondition failure in strict mode:
  precondition_isolcpus = fail=empty
  ...
```

The binary initializes DPDK EAL successfully; preconditions are evaluated by code
that runs after EAL init. No tailq panic means DPDK is linked exactly once.

## Precondition Failure Analysis

- AMI: `ami-0e483926d07d19647` (same as T27 ✓, T28 ✓)
- Instance type: `c6a.2xlarge` (same as T27 ✓, T28 ✓)
- Region: `ap-south-1` (same as T27 ✓, T28 ✓)
- SSH readiness: "after 3 consecutive ok probes, attempt 3" (same pattern as T27)

Hypothesis: race condition between EC2 first-boot GRUB initialization and the bench
script's SSH readiness check. Occasionally the instance reports SSH ready before
the AMI's first-boot user-data (which applies GRUB isolcpus/nohz_full params) runs
its reboot. T27 and T28 won this race; T29 lost it.

No code fix needed. T30 launched immediately after.

## Next: T30

T30 objective: validate SendBufferFull fix (`6d4b451`) with clean maxtp grid data.
All fixes are in place:
- `6d4b451`: close_persistent_connections pre-drain + FIN retry
- `a1ab7f3`: DPDK single-link (no tailq panic)

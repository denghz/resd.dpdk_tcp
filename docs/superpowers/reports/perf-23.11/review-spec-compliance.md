## Spec-compliance review — a10-perf-23.11

**Reviewer:** opus 4.7 spec-compliance subagent
**Branch:** `a10-perf-23.11`
**Worktree:** `/home/ubuntu/resd.dpdk_tcp-a10-perf`
**Spec:** `docs/superpowers/specs/2026-04-23-a10-microbench-perf-and-dpdk24-adopt-design.md`
**Reviewed commits:** 22 commits on `phase-a10..HEAD` (= `671062a..HEAD` against the
worktree's true base; `phase-a10` advanced past `671062a` after this worktree was rooted —
2 counter-related commits visible in `phase-a10..HEAD` that don't appear here are
phase-a10-only and not part of this branch's diff).

## Verdict

**PASS-WITH-CAVEATS**

Every spec-mandated check passes. The caveats are documented exits (host-ceiling on
`tsc_read`, deferred send family, diagnostic baseline) called out in the spec and
honored by the task's honesty-discipline guidance. No findings block the cherry-pick;
several findings are observational (`info`) or surface caveats the user should be aware
of when integrating onto master.

## Per-commit table

| Commit  | Subject                                                                                       | Spec link                                                  | Verdict |
|---------|-----------------------------------------------------------------------------------------------|------------------------------------------------------------|---------|
| 5eabf81 | a10-perf spec                                                                                 | spec itself (§0)                                           | PASS    |
| 2baadef | a10-perf plan                                                                                 | plan, derives from spec                                    | PASS    |
| 4cc34bd | perf-host-capabilities snapshot                                                               | §4.1, §8 risk row 1                                        | PASS    |
| a7f1fcd | scripts/check-perf-host.sh                                                                    | §4.2                                                       | PASS    |
| d3189ed | amend base commit 132e42a → 671062a                                                           | spec base-commit amendment header                          | PASS    |
| 797ab92 | add bench-internals cargo feature                                                             | §4.3, D3                                                   | PASS    |
| cd3cf20 | tcp_timer_wheel — gate pub(crate) under bench-internals                                       | §4.3                                                       | PASS    |
| 80ba3c1 | engine::test_support::EngineNoEalHarness                                                      | §4.3, D3                                                   | PASS    |
| cae8e01 | tests for EngineNoEalHarness                                                                  | §4.3 (correctness via test sweep, §8 risk row 2)           | PASS    |
| 7695b28 | bench-micro/poll — rewrite against EngineNoEalHarness                                         | §4.4                                                       | PASS    |
| 95f1bff | bench-micro/timer — rewrite against EngineNoEalHarness                                        | §4.4                                                       | PASS    |
| aeea1d6 | defer T2.7 bench_send_* real-path wiring to Phase 3 T3.3                                      | §5.1 hard-stop intent + plan §T2.7                         | PASS    |
| bfe3d99 | extend bench CSV with host + dpdk + worktree metadata                                         | §3.3, §4.4 ("New CSV columns")                             | PASS    |
| acedb33 | summarize — drop poll_* + timer_add_cancel from STUB_TARGETS                                  | §4.4                                                       | PASS    |
| d000ff9 | T3.0 cross-family baseline + opportunity matrix                                               | §5.1 step 1                                                | PASS    |
| 5b8ee71 | poll H1 — pre-allocate conn_handles_scratch (mirror Engine::poll_once)                        | §5.1 step 3 (Implement top hypothesis)                     | PASS    |
| 62b05f4 | poll — family summary (exit: gate-met)                                                        | §5.1 step 5, §5.4                                          | PASS    |
| 245ab8a | T3.2 + T3.4 + T3.5 + T3.7 family summaries (gate-met-at-baseline)                             | §5.1 step 9 first exit condition                           | PASS    |
| 2cc6829 | tsc_read H1+H2 — iter_custom batched + XOR-fold black_box                                     | §5.1 step 3 (methodology)                                  | PASS    |
| bf5f2a8 | tsc_read — family summary (exit: host-ceiling)                                                | §5.1 hard stop "documented as a known floor"               | PASS    |
| da319d6 | T3.3 send — family summary (exit: deferred-to-future-task)                                    | §5.1 hard-stop + plan T2.7 deferral                        | PASS-W-CAVEAT |
| 83e905e | T3.8 worktree-1 final summary                                                                 | §5.4 + §7.4                                                | PASS    |

## Per-finding table

| #  | Severity | Commit                | File:line                                                                                                  | Issue                                                                                                                                                                                                                                                                                                                                                       | Recommendation                                                                                                                                                                                                                                                       |
|----|----------|-----------------------|------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1  | caveat   | da319d6 + aeea1d6     | `docs/superpowers/reports/perf-23.11/send-summary.md`, `docs/superpowers/reports/t2-7-deferral.md`         | `bench_send_small` and `bench_send_large_chain` were deferred outright — no iteration cycle ran. Spec §5.4 "Exit criteria" says "Worktree 1 is **done** when all of: Every §11.2 target beaten per D5". Strictly read, "every target beaten" is unmet. Deferral is well-documented (rationale: EAL-init + hugepages + CAP_IPC_LOCK prerequisites), and §5.1 "hard stop" allows "Change would require public-API / ABI surface modification — stop, file as future spec-change candidate, move on" — defensible justification. | Accept the deferral as-documented for cherry-pick; explicitly mark `bench_send_*` as "not measured this effort" in the master integration commit. If/when the verification-host EAL-init wiring lands, reopen the family per the t2-7-deferral.md "What unblocks T3.3's execution" checklist. |
| 2  | caveat   | bf5f2a8               | `docs/superpowers/reports/perf-23.11/tsc_read-summary.md`                                                   | `bench_tsc_read_ffi` (10.22 ns) and `bench_tsc_read_inline` (10.16 ns) miss §11.2 (5 ns / 1 ns) by ~5–9 ns each. Documented as host-ceiling — KVM TSC virtualization adds ~5 ns of VMCS round-trip overhead the guest can't disable. Cross-validated against a pure-C `__rdtsc` tight-loop measuring 10.13 ns/op on the same host. §5.1 hard stop allows "documented as a known floor"; this is exactly that. | Acceptable as-is. When a bare-metal verification host runs, re-baseline this family — bare-metal Zen 3 should land in 2–5 ns range. Until then, treat `tsc_read-*` as host-ceiling-bounded sentinels.                                                            |
| 3  | caveat   | d000ff9 + all summaries | `docs/superpowers/reports/perf-23.11/opportunity-matrix.md`, every `<family>-summary.md`                   | Diagnostic baseline disclosed in every report: THP=`madvise` (target `never`), cpufreq governor not exposed under KVM (target `performance`), no `isolcpus`/`nohz_full`/`rcu_nocbs`. `scripts/check-perf-host.sh` exits 1 on this host. Numbers are stable for **relative** comparison (gate evaluation) but absolute numbers will tighten on a hardened host. Honesty-discipline guidance explicitly accepts this as caveat-not-block. | Re-baseline on a hardened verification host before publishing Worktree-1 ship numbers (also called out in summary.md). If the cherry-pick onto master gates regression alerts, anchor those alerts to hardened-host numbers, not these. |
| 4  | caveat   | 5b8ee71               | `docs/superpowers/reports/perf-23.11/poll-summary.md` §"uProf caveat carried forward"                      | H1 (the only retained Worktree-1 production-equivalent optimization) was hypothesized from code-reading rather than from uProf TBP attribution. uProf TBP samples at 1 ms; the workload is sub-µs, so symbol-level attribution wasn't possible. The hypothesis is supported by **measured** evidence: criterion median dropped 95.5% (1031.5 ns → 45.8 ns) with p<0.05. Per task brief, this constitutes valid measurable evidence for this host. | None. The H1 win is real and reproducible. If uProf gains workload-symbol resolution on a future host (bare-metal, PMC-virtualized, or via `perf` software events), retroactively confirm the hot-path attribution.                                                       |
| 5  | caveat   | (any commit on branch) | spec §5.1 step 5 ("re-capture uProf, commit `<family>-iter-N.md`")                                          | Spec §5.1 step 5 prescribes per-iteration `<family>-iter-N.md` reports for each retained optimization; the branch's per-family work consolidated all iterations into a single `<family>-summary.md`. Because uProf TBP couldn't attribute symbols on this host, the per-iteration "new hotspot list" the spec asks for would have been empty data. | Acceptable. The summary files contain the same information consolidated — final medians, retained optimizations, rejected hypotheses, exit reason. If a future host has IBS/PMC, future iterations should produce per-iteration reports per spec step 5.            |
| 6  | info     | 245ab8a + each gate-met family summary | `tcp_input-summary.md`, `flow_lookup-summary.md`, `timer-summary.md`, `counters-summary.md`               | These four summaries report criterion median vs §11.2 upper but don't reproduce p99-vs-2×-upper inline. The p99 values are present in the T3.0 baseline opportunity-matrix.md (within 2× upper for all four), and the summaries note "gate-met-at-baseline (P1 step 9 first condition)" which is sufficient under spec D5. Pure presentation; the rule is satisfied. | Optional polish: cross-reference the matrix's p99 row directly inside each `<family>-summary.md` for stand-alone readability. Not required for spec compliance.                                                                                                       |
| 7  | info     | 797ab92, 80ba3c1, cd3cf20 | `crates/dpdk-net-core/Cargo.toml`, `crates/dpdk-net-core/src/lib.rs`, `crates/dpdk-net-core/src/engine.rs` | Public-API/ABI surface check: only the spec-approved `bench-internals` feature lands. `EngineNoEalHarness` lives entirely under `#[cfg(feature = "bench-internals")]`; `tcp_timer_wheel` module promotion is dual-cfg (`pub(crate)` default vs `pub` under feature). Default `cargo build` is byte-identical to pre-change (verified by commit message of cd3cf20). `feedback_counter_policy`: zero new counters added. `counters.rs` itself is untouched on this branch (the spec's post-brainstorm clarification permits hot-path counters with policy compliance, but no counter was actually needed). | None. Surface remains within the spec D3 envelope.                                                                                                                                                                                                                  |
| 8  | info     | (branch)              | spec §2.2 "Out of scope: `preset=rfc_compliance` wire-behavior changes"                                    | No production wire-behavior changes on the branch. `feedback_trading_latency_defaults` not violated. `feedback_performance_first_flow_control` not violated (no peer-throttling, no counter-throttling feedback loop introduced — engine.rs production code is untouched). Only the bench harness and the `EngineNoEalHarness` surrogate were modified. | None.                                                                                                                                                                                                                                                                |

## Counter-policy compliance summary

| Check                                                                                                       | Result | Evidence                                                                                                                |
|-------------------------------------------------------------------------------------------------------------|--------|-------------------------------------------------------------------------------------------------------------------------|
| Hot-path counters added without compile-time feature gate?                                                  | No     | `git diff 671062a..HEAD -- crates/dpdk-net-core/` shows zero modifications to `counters.rs` and zero new `fetch_add` sites in production code paths. |
| Hot-path counters added without batched increment?                                                          | No     | (no counters added)                                                                                                     |
| Hot-path counters added without documented justification?                                                   | No     | (no counters added)                                                                                                     |

The spec's post-brainstorm clarification "new production hot-path counters are allowed if measurement justifies them" is **observed-in-letter, not invoked-in-deed**: the effort never reached a measurement-justified counter addition because the families that were optimized (poll, tsc_read) and those left at baseline (tcp_input, flow_lookup, timer, counters) all met the gate without observability changes.

## §11.2 target compliance summary

| Bench                          | Final median | §11.2 upper | Median ≤ upper? | Final p99 (or per opp-matrix) | 2× upper | p99 ≤ 2× upper? | Evidence                            |
|--------------------------------|--------------|-------------|-----------------|-------------------------------|----------|-----------------|-------------------------------------|
| `bench_poll_empty`             | 45.8 ns      | 100 ns      | ✓               | 50.32 ns                      | 200 ns   | ✓               | poll-summary.md, T3.1 H1            |
| `bench_poll_idle_with_timers`  | 50.7 ns      | 100 ns      | ✓               | 57.87 ns                      | 200 ns   | ✓               | poll-summary.md, T3.1 H1            |
| `bench_tcp_input_data_segment` | 86.53 ns     | 200 ns      | ✓               | 113.68 ns (T3.0)              | 400 ns   | ✓               | tcp_input-summary.md (gate-at-base) |
| `bench_tcp_input_ooo_segment`  | 75.54 ns     | 400 ns      | ✓               | 127.97 ns (T3.0)              | 800 ns   | ✓               | tcp_input-summary.md                |
| `bench_flow_lookup_hot`        | 25.88 ns     | 40 ns       | ✓               | 31.36 ns (T3.0)               | 80 ns    | ✓               | flow_lookup-summary.md              |
| `bench_flow_lookup_cold`       | 89.64 ns     | 200 ns      | ✓               | 145.23 ns (T3.0)              | 400 ns   | ✓               | flow_lookup-summary.md              |
| `bench_timer_add_cancel`       | 23.94 ns     | 50 ns       | ✓               | 40.03 ns (T3.0)               | 100 ns   | ✓               | timer-summary.md                    |
| `bench_counters_read`          | 0.92 ns      | 100 ns      | ✓               | 1.91 ns (T3.0)                | 200 ns   | ✓               | counters-summary.md                 |
| `bench_tsc_read_ffi`           | 10.22 ns     | 5 ns        | ✗ host-ceiling  | 10.34 ns                      | 10 ns    | ✗ host-ceiling  | tsc_read-summary.md, C-reproduction |
| `bench_tsc_read_inline`        | 10.16 ns     | 1 ns        | ✗ host-ceiling  | 10.35 ns                      | 2 ns     | ✗ host-ceiling  | tsc_read-summary.md, C-reproduction |
| `bench_send_small`             | (stub)       | 150 ns      | n/a deferred    | n/a                           | 300 ns   | n/a             | send-summary.md, t2-7-deferral.md   |
| `bench_send_large_chain`       | (stub)       | 5000 ns     | n/a deferred    | n/a                           | 10000 ns | n/a             | send-summary.md, t2-7-deferral.md   |

**Result:** 8 of 12 benches strictly within budget on this host. 2 of 12 host-ceiling-bound (tsc_read both variants — KVM TSC virt). 2 of 12 deferred (bench_send_*). The host-ceiling and deferral are explicit spec-recognized exits (§5.1 hard-stop and "documented as a known floor"), not gate failures. Per the prompt's honesty discipline, these are caveats rather than blocks.

## Notes

- **Commit-range disambiguation.** The task brief asked for `git log --oneline phase-a10..HEAD`. That range is correct *as a documentation reference* (the worktree was branched from a commit that was on `phase-a10` at that time), but `phase-a10` has advanced two commits past the worktree's true base `671062a`. The spec's base-commit amendment commit (`d3189ed`) documents this. The 22 commits on `phase-a10..HEAD` are **identical** to the 22 commits on `671062a..HEAD`; the only difference is that `phase-a10..HEAD` *additionally* shows two phase-a10-only commits as **negative deltas** (i.e. counter fields appear to be removed). Those phantom deletions are not part of this branch and should not be cherry-picked back onto master from this branch.

- **Exploratory landing policy + per-task discipline.** Spec D2 explicitly waives per-change `mTCP-comparison-reviewer` / `rfc-compliance-reviewer` / `code-reviewer` reviews during iteration in favor of the end-of-effort two-stage gate. This is consistent with `feedback_per_task_review_discipline` because the spec recognizes the per-task discipline and substitutes an end-of-effort aggregate review with the same two reviewers (this one + the parallel code-quality reviewer). Worktree branches stay local research references; only what survives end-of-effort review cherry-picks onto master.

- **Public-API surface fully respects the §4.3 envelope.** The only library-side surface added is the `bench-internals` feature flag and its dual-cfg gates around `tcp_timer_wheel`'s module visibility plus the new `engine::test_support::EngineNoEalHarness`. Default-feature builds compile to byte-identical output (verified in cd3cf20's commit message). The C ABI (cbindgen-generated `dpdk_net.h`) is not touched on this branch — verified by `git diff 671062a..HEAD -- crates/dpdk-net-sys/`.

- **Bench harness changes (CSV schema, summarize, STUB_TARGETS) are bench-side only.** No production code path consumes these. Pre-existing bench tools that don't yet populate the new CSV columns emit blank cells (verified in bfe3d99's diff against `tools/bench-{ab-runner,e2e,offload-ab,report,stress,vs-linux,vs-mtcp}/`).

- **uProf evidence honesty.** Every family summary discloses the KVM TBP-only limitation (per `perf-host-capabilities.md`). The retained optimizations are either (a) measured by criterion with p<0.05 — H1 poll harness scratch reuse (criterion -95%); H2 tsc_read XOR-fold (criterion -1.7%) — or (b) measurement-methodology only with no numerical effect (H1 tsc_read iter_custom). No retained optimization claims uProf attribution it doesn't have.

- **No hot-path counter addition was needed.** Spec post-brainstorm clarification permits them under `feedback_counter_policy`; none ended up justified. The branch landed zero counter-related changes.

- **Cherry-pick set for master integration.** The branch's own summary.md correctly identifies the cherry-pick candidates (797ab92, cd3cf20, 80ba3c1, cae8e01, 5b8ee71, 7695b285, 95f1bff, 2cc6829, bfe3d99, acedb33) — all bench-internals-gated or doc/script-only — and explicitly notes "no commit introduces production behavior change". The reviewer concurs.

- **Ship-readiness.** This branch is a clean exploratory landing per spec D2: the `bench-internals` feature ships as opt-in scaffolding; harness measurement instruments improve; one production-equivalent optimization (H1 poll, mirroring the real `Engine::poll_once`'s already-shipped pattern) is captured for future port-forward. End-of-effort review (this report + the code-quality reviewer's parallel report) is the gate. From spec-compliance standpoint, the cherry-pick set is good to land once the code-quality review concurs.
